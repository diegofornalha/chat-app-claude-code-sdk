# Kingston Enhanced System - Guia de Deployment\n\n## 🚀 Visão Geral\n\nEste guia fornece instruções completas para fazer o deployment do Kingston Enhanced System em produção, incluindo configuração, monitoramento e otimização.\n\n## 📋 Pré-requisitos\n\n### Sistema Operacional\n- Ubuntu 20.04 LTS ou superior\n- CentOS 8 ou superior\n- Docker (recomendado)\n\n### Software\n- Node.js 18.x ou superior\n- npm 8.x ou superior\n- Redis 6.x ou superior\n- PostgreSQL 13.x ou superior (opcional)\n- PM2 (para gerenciamento de processos)\n- Nginx (para proxy reverso)\n\n### Hardware Mínimo\n\n#### Desenvolvimento\n- CPU: 2 cores\n- RAM: 4 GB\n- Storage: 20 GB SSD\n\n#### Produção\n- CPU: 4 cores\n- RAM: 8 GB\n- Storage: 100 GB SSD\n- Rede: 1 Gbps\n\n#### Alta Performance\n- CPU: 8+ cores\n- RAM: 16+ GB\n- Storage: 500+ GB NVMe SSD\n- Rede: 10 Gbps\n\n## 🐳 Deployment com Docker (Recomendado)\n\n### 1. Preparação do Ambiente\n\n```bash\n# Clone o repositório\ngit clone https://github.com/your-org/kingston-enhanced.git\ncd kingston-enhanced\n\n# Configurar variáveis de ambiente\ncp .env.example .env\n```\n\n### 2. Configuração do Docker Compose\n\n**docker-compose.prod.yml:**\n```yaml\nversion: '3.8'\n\nservices:\n  # Backend Enhanced\n  kingston-backend:\n    build:\n      context: ./backend\n      dockerfile: Dockerfile.prod\n    ports:\n      - \"8080:8080\"\n    environment:\n      - NODE_ENV=production\n      - REDIS_URL=redis://redis:6379\n      - POSTGRES_URL=postgresql://user:password@postgres:5432/kingston\n      - CLAUDE_CODE_API_KEY=${CLAUDE_CODE_API_KEY}\n      - USE_AI_SDK_V5=true\n      - ORCHESTRATOR_ENABLED=true\n      - QUALITY_THRESHOLD=7.0\n      - MAX_CONCURRENCY=10\n      - TELEMETRY_ENABLED=true\n    volumes:\n      - ./logs:/app/logs\n      - ./uploads:/app/uploads\n    depends_on:\n      - redis\n      - postgres\n    restart: unless-stopped\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8080/api/health\"]\n      interval: 30s\n      timeout: 10s\n      retries: 3\n\n  # Frontend\n  kingston-frontend:\n    build:\n      context: ./frontend\n      dockerfile: Dockerfile.prod\n    ports:\n      - \"3000:80\"\n    environment:\n      - REACT_APP_API_BASE_URL=http://kingston-backend:8080\n      - REACT_APP_ENABLE_METRICS=true\n      - REACT_APP_ENABLE_TASK_EXECUTION=true\n    depends_on:\n      - kingston-backend\n    restart: unless-stopped\n\n  # Redis para cache e sessões\n  redis:\n    image: redis:7-alpine\n    ports:\n      - \"6379:6379\"\n    volumes:\n      - redis_data:/data\n    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru\n    restart: unless-stopped\n\n  # PostgreSQL para dados persistentes\n  postgres:\n    image: postgres:15-alpine\n    ports:\n      - \"5432:5432\"\n    environment:\n      - POSTGRES_DB=kingston\n      - POSTGRES_USER=kingston\n      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql\n    restart: unless-stopped\n\n  # Nginx Proxy Reverso\n  nginx:\n    image: nginx:alpine\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx/nginx.conf:/etc/nginx/nginx.conf\n      - ./nginx/ssl:/etc/nginx/ssl\n      - ./logs/nginx:/var/log/nginx\n    depends_on:\n      - kingston-frontend\n      - kingston-backend\n    restart: unless-stopped\n\n  # Monitoring\n  prometheus:\n    image: prom/prometheus:latest\n    ports:\n      - \"9090:9090\"\n    volumes:\n      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml\n      - prometheus_data:/prometheus\n    command:\n      - '--config.file=/etc/prometheus/prometheus.yml'\n      - '--storage.tsdb.path=/prometheus'\n      - '--web.console.libraries=/etc/prometheus/console_libraries'\n      - '--web.console.templates=/etc/prometheus/consoles'\n    restart: unless-stopped\n\n  # Grafana\n  grafana:\n    image: grafana/grafana:latest\n    ports:\n      - \"3001:3000\"\n    environment:\n      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}\n    volumes:\n      - grafana_data:/var/lib/grafana\n      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards\n      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources\n    restart: unless-stopped\n\nvolumes:\n  redis_data:\n  postgres_data:\n  prometheus_data:\n  grafana_data:\n\nnetworks:\n  default:\n    driver: bridge\n```\n\n### 3. Configuração de Variáveis de Ambiente\n\n**.env:**\n```bash\n# API Keys\nCLAUDE_CODE_API_KEY=your_claude_code_api_key\nSTRIPE_SECRET_KEY=your_stripe_secret_key\n\n# Database\nPOSTGRES_PASSWORD=secure_password_here\n\n# Security\nJWT_SECRET=your_jwt_secret_key\nSESSION_SECRET=your_session_secret\n\n# Enhanced Features\nUSE_AI_SDK_V5=true\nORCHESTRATOR_ENABLED=true\nQUALITY_THRESHOLD=7.0\nMAX_CONCURRENCY=10\nTELEMETRY_ENABLED=true\nSTRUCTURED_OUTPUT_ENABLED=true\n\n# Performance\nMAX_WORKERS=5\nWORKER_TIMEOUT=30000\nMETRICS_INTERVAL=30000\nCACHE_TTL=300000\n\n# Monitoring\nGRAFANA_PASSWORD=admin_password_here\nPROMETHEUS_ENABLED=true\n\n# Security\nALLOWED_DOMAINS=yourdomain.com,*.yourdomain.com\nENABLE_RATE_LIMITING=true\nMAX_REQUESTS_PER_MINUTE=100\n\n# SSL\nSSL_ENABLED=true\nSSL_CERT_PATH=/etc/nginx/ssl/cert.pem\nSSL_KEY_PATH=/etc/nginx/ssl/key.pem\n```\n\n### 4. Dockerfile do Backend\n\n**backend/Dockerfile.prod:**\n```dockerfile\nFROM node:18-alpine AS builder\n\nWORKDIR /app\n\n# Install dependencies\nCOPY package*.json ./\nRUN npm ci --only=production && npm cache clean --force\n\n# Copy source code\nCOPY . .\n\n# Create non-root user\nRUN addgroup -g 1001 -S nodejs\nRUN adduser -S nodejs -u 1001\n\n# Set permissions\nRUN chown -R nodejs:nodejs /app\nUSER nodejs\n\n# Health check\nHEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \\\n  CMD node healthcheck.js\n\nEXPOSE 8080\n\nCMD [\"node\", \"server.js\"]\n```\n\n### 5. Dockerfile do Frontend\n\n**frontend/Dockerfile.prod:**\n```dockerfile\nFROM node:18-alpine AS builder\n\nWORKDIR /app\n\n# Install dependencies\nCOPY package*.json ./\nRUN npm ci\n\n# Copy source and build\nCOPY . .\nRUN npm run build\n\n# Production stage\nFROM nginx:alpine\n\n# Copy built app\nCOPY --from=builder /app/build /usr/share/nginx/html\n\n# Copy nginx config\nCOPY nginx.conf /etc/nginx/conf.d/default.conf\n\nEXPOSE 80\n\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n\n### 6. Configuração do Nginx\n\n**nginx/nginx.conf:**\n```nginx\nuser nginx;\nworker_processes auto;\nerror_log /var/log/nginx/error.log warn;\npid /var/run/nginx.pid;\n\nevents {\n    worker_connections 1024;\n    use epoll;\n    multi_accept on;\n}\n\nhttp {\n    include /etc/nginx/mime.types;\n    default_type application/octet-stream;\n\n    # Logging\n    log_format main '$remote_addr - $remote_user [$time_local] \"$request\" '\n                    '$status $body_bytes_sent \"$http_referer\" '\n                    '\"$http_user_agent\" \"$http_x_forwarded_for\" '\n                    'rt=$request_time uct=\"$upstream_connect_time\" '\n                    'uht=\"$upstream_header_time\" urt=\"$upstream_response_time\"';\n\n    access_log /var/log/nginx/access.log main;\n\n    # Performance\n    sendfile on;\n    tcp_nopush on;\n    tcp_nodelay on;\n    keepalive_timeout 65;\n    types_hash_max_size 2048;\n    client_max_body_size 10M;\n\n    # Compression\n    gzip on;\n    gzip_vary on;\n    gzip_min_length 1024;\n    gzip_types\n        application/javascript\n        application/json\n        application/xml\n        text/css\n        text/javascript\n        text/plain\n        text/xml;\n\n    # Rate limiting\n    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;\n    limit_req_zone $binary_remote_addr zone=websocket:10m rate=5r/s;\n\n    # Upstream servers\n    upstream backend {\n        server kingston-backend:8080;\n        keepalive 32;\n    }\n\n    upstream frontend {\n        server kingston-frontend:80;\n        keepalive 32;\n    }\n\n    # HTTP to HTTPS redirect\n    server {\n        listen 80;\n        server_name _;\n        return 301 https://$host$request_uri;\n    }\n\n    # Main server block\n    server {\n        listen 443 ssl http2;\n        server_name yourdomain.com;\n\n        # SSL Configuration\n        ssl_certificate /etc/nginx/ssl/cert.pem;\n        ssl_certificate_key /etc/nginx/ssl/key.pem;\n        ssl_protocols TLSv1.2 TLSv1.3;\n        ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;\n        ssl_prefer_server_ciphers off;\n        ssl_session_cache shared:SSL:10m;\n        ssl_session_timeout 10m;\n\n        # Security headers\n        add_header Strict-Transport-Security \"max-age=31536000; includeSubDomains\" always;\n        add_header X-Content-Type-Options nosniff;\n        add_header X-Frame-Options DENY;\n        add_header X-XSS-Protection \"1; mode=block\";\n        add_header Referrer-Policy \"no-referrer-when-downgrade\";\n\n        # Frontend\n        location / {\n            proxy_pass http://frontend;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            \n            # Cache static assets\n            location ~* \\.(js|css|png|jpg|jpeg|gif|ico|svg)$ {\n                expires 1y;\n                add_header Cache-Control \"public, immutable\";\n            }\n        }\n\n        # API routes\n        location /api/ {\n            limit_req zone=api burst=20 nodelay;\n            \n            proxy_pass http://backend;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            \n            # Timeouts\n            proxy_connect_timeout 60s;\n            proxy_send_timeout 60s;\n            proxy_read_timeout 60s;\n        }\n\n        # WebSocket support\n        location /socket.io/ {\n            limit_req zone=websocket burst=10 nodelay;\n            \n            proxy_pass http://backend;\n            proxy_http_version 1.1;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection \"upgrade\";\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            \n            # WebSocket timeouts\n            proxy_read_timeout 86400;\n        }\n\n        # Health checks\n        location /health {\n            access_log off;\n            proxy_pass http://backend/api/health;\n        }\n    }\n}\n```\n\n### 7. Deploy\n\n```bash\n# Build e start dos containers\ndocker-compose -f docker-compose.prod.yml up -d --build\n\n# Verificar status\ndocker-compose -f docker-compose.prod.yml ps\n\n# Verificar logs\ndocker-compose -f docker-compose.prod.yml logs -f kingston-backend\n\n# Health check\ncurl -f http://localhost:8080/api/health\n```\n\n## 🔧 Deployment Tradicional (sem Docker)\n\n### 1. Preparação do Servidor\n\n```bash\n# Update sistema\nsudo apt update && sudo apt upgrade -y\n\n# Install Node.js 18\ncurl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -\nsudo apt-get install -y nodejs\n\n# Install PM2\nsudo npm install -g pm2\n\n# Install Redis\nsudo apt install redis-server\n\n# Install Nginx\nsudo apt install nginx\n```\n\n### 2. Deploy do Backend\n\n```bash\n# Clone repositório\ngit clone https://github.com/your-org/kingston-enhanced.git\ncd kingston-enhanced/backend\n\n# Install dependencies\nnpm ci --production\n\n# Configurar environment\ncp .env.example .env\n# Editar .env com valores de produção\n\n# Create ecosystem file for PM2\ncat > ecosystem.config.js << EOF\nmodule.exports = {\n  apps: [{\n    name: 'kingston-backend',\n    script: 'server.js',\n    instances: 'max',\n    exec_mode: 'cluster',\n    env: {\n      NODE_ENV: 'production',\n      PORT: 8080\n    },\n    error_file: './logs/err.log',\n    out_file: './logs/out.log',\n    log_file: './logs/combined.log',\n    time: true,\n    max_memory_restart: '1G',\n    node_args: '--max-old-space-size=1024'\n  }]\n};\nEOF\n\n# Start com PM2\npm2 start ecosystem.config.js\npm2 save\npm2 startup\n```\n\n### 3. Deploy do Frontend\n\n```bash\ncd ../frontend\n\n# Install dependencies\nnpm ci\n\n# Build production\nnpm run build\n\n# Copy para nginx\nsudo cp -r build/* /var/www/html/\nsudo chown -R www-data:www-data /var/www/html/\n```\n\n### 4. Configuração do Nginx\n\n```bash\n# Criar configuração do site\nsudo cat > /etc/nginx/sites-available/kingston << EOF\nserver {\n    listen 80;\n    server_name yourdomain.com;\n    \n    root /var/www/html;\n    index index.html;\n    \n    location / {\n        try_files \\$uri \\$uri/ /index.html;\n    }\n    \n    location /api/ {\n        proxy_pass http://localhost:8080;\n        proxy_set_header Host \\$host;\n        proxy_set_header X-Real-IP \\$remote_addr;\n        proxy_set_header X-Forwarded-For \\$proxy_add_x_forwarded_for;\n    }\n    \n    location /socket.io/ {\n        proxy_pass http://localhost:8080;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade \\$http_upgrade;\n        proxy_set_header Connection \"upgrade\";\n    }\n}\nEOF\n\n# Enable site\nsudo ln -s /etc/nginx/sites-available/kingston /etc/nginx/sites-enabled/\nsudo nginx -t\nsudo systemctl reload nginx\n```\n\n## 🔐 Configuração SSL\n\n### Com Certbot (Let's Encrypt)\n\n```bash\n# Install certbot\nsudo apt install certbot python3-certbot-nginx\n\n# Obter certificado\nsudo certbot --nginx -d yourdomain.com\n\n# Auto-renovação\nsudo crontab -e\n# Adicionar: 0 12 * * * /usr/bin/certbot renew --quiet\n```\n\n### SSL Personalizado\n\n```bash\n# Copiar certificados\nsudo mkdir -p /etc/nginx/ssl\nsudo cp your-cert.pem /etc/nginx/ssl/cert.pem\nsudo cp your-key.pem /etc/nginx/ssl/key.pem\nsudo chmod 600 /etc/nginx/ssl/*\n```\n\n## 📊 Monitoramento\n\n### 1. Configuração do Prometheus\n\n**monitoring/prometheus.yml:**\n```yaml\nglobal:\n  scrape_interval: 15s\n  evaluation_interval: 15s\n\nscrape_configs:\n  - job_name: 'kingston-backend'\n    static_configs:\n      - targets: ['kingston-backend:8080']\n    metrics_path: '/api/metrics'\n    scrape_interval: 5s\n\n  - job_name: 'node-exporter'\n    static_configs:\n      - targets: ['localhost:9100']\n\n  - job_name: 'redis'\n    static_configs:\n      - targets: ['redis:6379']\n\n  - job_name: 'postgres'\n    static_configs:\n      - targets: ['postgres:5432']\n```\n\n### 2. Dashboards do Grafana\n\n**monitoring/grafana/dashboards/kingston.json:**\n```json\n{\n  \"dashboard\": {\n    \"title\": \"Kingston Enhanced System\",\n    \"panels\": [\n      {\n        \"title\": \"Request Rate\",\n        \"type\": \"graph\",\n        \"targets\": [\n          {\n            \"expr\": \"rate(http_requests_total[5m])\",\n            \"legendFormat\": \"{{method}} {{status}}\"\n          }\n        ]\n      },\n      {\n        \"title\": \"Response Time\",\n        \"type\": \"graph\",\n        \"targets\": [\n          {\n            \"expr\": \"histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))\",\n            \"legendFormat\": \"95th percentile\"\n          }\n        ]\n      },\n      {\n        \"title\": \"Enhanced Agent Metrics\",\n        \"type\": \"graph\",\n        \"targets\": [\n          {\n            \"expr\": \"enhanced_agent_tasks_total\",\n            \"legendFormat\": \"Total Tasks\"\n          },\n          {\n            \"expr\": \"enhanced_agent_success_rate\",\n            \"legendFormat\": \"Success Rate\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n### 3. Alertas\n\n**monitoring/alerts.yml:**\n```yaml\ngroups:\n  - name: kingston-alerts\n    rules:\n      - alert: HighErrorRate\n        expr: rate(http_requests_total{status=~\"5..\"}[5m]) > 0.1\n        for: 5m\n        labels:\n          severity: critical\n        annotations:\n          summary: \"High error rate detected\"\n          description: \"Error rate is {{ $value }} errors per second\"\n\n      - alert: HighResponseTime\n        expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 2\n        for: 5m\n        labels:\n          severity: warning\n        annotations:\n          summary: \"High response time detected\"\n          description: \"95th percentile response time is {{ $value }}s\"\n\n      - alert: EnhancedAgentDown\n        expr: enhanced_agent_status == 0\n        for: 1m\n        labels:\n          severity: critical\n        annotations:\n          summary: \"Enhanced Agent Manager is down\"\n          description: \"Enhanced Agent Manager has been down for more than 1 minute\"\n```\n\n## 🔄 CI/CD Pipeline\n\n### GitHub Actions\n\n**.github/workflows/deploy.yml:**\n```yaml\nname: Deploy to Production\n\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    \n    services:\n      redis:\n        image: redis:7-alpine\n        ports:\n          - 6379:6379\n    \n    steps:\n    - uses: actions/checkout@v3\n    \n    - name: Setup Node.js\n      uses: actions/setup-node@v3\n      with:\n        node-version: '18'\n        cache: 'npm'\n        cache-dependency-path: '**/package-lock.json'\n    \n    - name: Install Backend Dependencies\n      working-directory: ./backend\n      run: npm ci\n    \n    - name: Install Frontend Dependencies\n      working-directory: ./frontend\n      run: npm ci\n    \n    - name: Run Backend Tests\n      working-directory: ./backend\n      run: npm test\n      env:\n        NODE_ENV: test\n        REDIS_URL: redis://localhost:6379\n    \n    - name: Run Frontend Tests\n      working-directory: ./frontend\n      run: npm test -- --coverage --watchAll=false\n    \n    - name: Build Frontend\n      working-directory: ./frontend\n      run: npm run build\n    \n    - name: Run E2E Tests\n      working-directory: ./backend\n      run: npm run test:e2e\n  \n  deploy:\n    needs: test\n    runs-on: ubuntu-latest\n    if: github.ref == 'refs/heads/main'\n    \n    steps:\n    - uses: actions/checkout@v3\n    \n    - name: Deploy to Production\n      uses: appleboy/ssh-action@v0.1.5\n      with:\n        host: ${{ secrets.DEPLOY_HOST }}\n        username: ${{ secrets.DEPLOY_USER }}\n        key: ${{ secrets.DEPLOY_KEY }}\n        script: |\n          cd /opt/kingston-enhanced\n          git pull origin main\n          docker-compose -f docker-compose.prod.yml down\n          docker-compose -f docker-compose.prod.yml up -d --build\n          \n          # Health check\n          sleep 30\n          curl -f http://localhost:8080/api/health || exit 1\n          \n          # Cleanup old images\n          docker image prune -f\n```\n\n## 🛡️ Segurança\n\n### 1. Firewall\n\n```bash\n# Configure UFW\nsudo ufw default deny incoming\nsudo ufw default allow outgoing\nsudo ufw allow ssh\nsudo ufw allow 80\nsudo ufw allow 443\nsudo ufw enable\n```\n\n### 2. Hardening do Sistema\n\n```bash\n# Disable root login\nsudo sed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config\n\n# Change SSH port\nsudo sed -i 's/#Port 22/Port 2222/' /etc/ssh/sshd_config\n\n# Restart SSH\nsudo systemctl restart ssh\n\n# Update UFW\nsudo ufw delete allow ssh\nsudo ufw allow 2222\n```\n\n### 3. Backup\n\n```bash\n# Criar script de backup\ncat > /opt/backup-kingston.sh << 'EOF'\n#!/bin/bash\n\nDATE=$(date +%Y%m%d_%H%M%S)\nBACKUP_DIR=\"/opt/backups/kingston\"\n\n# Create backup directory\nmkdir -p $BACKUP_DIR\n\n# Backup database\ndocker exec postgres pg_dump -U kingston kingston > $BACKUP_DIR/db_$DATE.sql\n\n# Backup uploads\ntar -czf $BACKUP_DIR/uploads_$DATE.tar.gz /opt/kingston-enhanced/uploads\n\n# Backup config\ncp /opt/kingston-enhanced/.env $BACKUP_DIR/env_$DATE.backup\n\n# Clean old backups (keep 7 days)\nfind $BACKUP_DIR -name \"*\" -mtime +7 -delete\n\necho \"Backup completed: $DATE\"\nEOF\n\n# Make executable\nchmod +x /opt/backup-kingston.sh\n\n# Add to crontab\necho \"0 2 * * * /opt/backup-kingston.sh\" | sudo crontab -\n```\n\n## 📈 Otimização de Performance\n\n### 1. Configuração do Node.js\n\n```bash\n# .nvmrc\n18.17.0\n\n# ecosystem.config.js\nmodule.exports = {\n  apps: [{\n    name: 'kingston-backend',\n    script: 'server.js',\n    instances: 'max',\n    exec_mode: 'cluster',\n    max_memory_restart: '1G',\n    node_args: [\n      '--max-old-space-size=1024',\n      '--optimize-for-size',\n      '--gc-interval=100'\n    ],\n    env_production: {\n      NODE_ENV: 'production',\n      UV_THREADPOOL_SIZE: 16\n    }\n  }]\n};\n```\n\n### 2. Redis Optimization\n\n```bash\n# redis.conf\nmaxmemory 1gb\nmaxmemory-policy allkeys-lru\ntcp-keepalive 60\ntimeout 300\n\n# Restart redis\nsudo systemctl restart redis\n```\n\n### 3. PostgreSQL Tuning\n\n```sql\n-- postgresql.conf\nshared_buffers = 256MB\neffective_cache_size = 1GB\nwork_mem = 4MB\nmaintenance_work_mem = 64MB\nmax_connections = 100\n```\n\n## 🚨 Troubleshooting\n\n### Logs\n\n```bash\n# Backend logs\ndocker-compose logs -f kingston-backend\n\n# Nginx logs\nsudo tail -f /var/log/nginx/error.log\n\n# System logs\nsudo journalctl -f -u kingston-backend\n```\n\n### Diagnósticos Comuns\n\n#### High Memory Usage\n```bash\n# Check memory usage\ndocker stats\n\n# Restart services\ndocker-compose restart kingston-backend\n\n# Check Node.js heap\nnode --inspect server.js\n```\n\n#### Database Connection Issues\n```bash\n# Test PostgreSQL connection\ndocker exec -it postgres psql -U kingston -d kingston -c \"SELECT 1;\"\n\n# Test Redis connection\ndocker exec -it redis redis-cli ping\n```\n\n#### Performance Issues\n```bash\n# Enable Node.js profiling\nnode --prof server.js\n\n# Check CPU usage\ntop -p $(pgrep -f \"node.*server.js\")\n\n# Memory profiling\nnode --inspect --inspect-brk server.js\n```\n\n## 📊 Métricas de Sucesso\n\n### KPIs de Produção\n\n1. **Availability**: > 99.9%\n2. **Response Time**: < 2s (P95)\n3. **Error Rate**: < 1%\n4. **Throughput**: > 1000 req/min\n5. **Memory Usage**: < 80%\n6. **CPU Usage**: < 70%\n\n### Health Checks\n\n```bash\n# Automated health check\ncurl -f http://localhost:8080/api/health\n\n# Enhanced components check\ncurl http://localhost:8080/api/enhanced/status\n\n# Metrics check\ncurl http://localhost:8080/api/telemetry/metrics\n```\n\n### Alertas Críticos\n\n1. Service Down\n2. High Error Rate (> 5%)\n3. High Response Time (> 5s)\n4. Memory Usage (> 90%)\n5. Disk Space (> 85%)\n6. Enhanced Agent Failure\n\n## 🔄 Manutenção\n\n### Updates\n\n```bash\n# Update dependencies\nnpm audit fix\n\n# Update Docker images\ndocker-compose pull\ndocker-compose up -d\n\n# Update system\nsudo apt update && sudo apt upgrade\n```\n\n### Scaling\n\n```bash\n# Scale backend horizontally\ndocker-compose up -d --scale kingston-backend=3\n\n# Scale workers\ndocker-compose exec kingston-backend npm run scale-workers 10\n```\n\nEste guia fornece uma base sólida para deployment em produção do Kingston Enhanced System. Ajuste as configurações conforme necessário para seu ambiente específico."